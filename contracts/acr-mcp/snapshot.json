{
  "captured_at": "2026-09-21T20:47:11Z",
  "host": "mcp.fullchaos.dev",
  "negotiations": [
    {
      "negotiated": "2026-07-28",
      "path": "server/discover",
      "requested": "2026-07-28"
    },
    {
      "negotiated": "2025-06-18",
      "path": "initialize",
      "requested": "2025-06-18"
    }
  ],
  "prompts": [
    {
      "arguments": [
        {
          "name": "parent_result_id",
          "required": true
        },
        {
          "name": "question",
          "required": true
        },
        {
          "name": "receipts",
          "required": false
        }
      ],
      "name": "continue_investigation"
    },
    {
      "arguments": [
        {
          "name": "evidence_ref_id",
          "required": true
        }
      ],
      "name": "expand_evidence"
    },
    {
      "arguments": [
        {
          "name": "expected_kinds",
          "required": false
        },
        {
          "name": "project",
          "required": false
        },
        {
          "name": "question",
          "required": true
        },
        {
          "name": "repository",
          "required": false
        },
        {
          "name": "team",
          "required": false
        },
        {
          "name": "window",
          "required": false
        }
      ],
      "name": "investigate"
    }
  ],
  "resources": [
    {
      "name": "guide-conversation",
      "uri": "acr://guide/conversation"
    },
    {
      "name": "guide-questions",
      "uri": "acr://guide/questions"
    },
    {
      "name": "guide-vocabulary",
      "uri": "acr://guide/vocabulary"
    }
  ],
  "schema_version": "mcp_tools.v1",
  "server_info": {
    "name": "dev-health-acr-mcp",
    "title": "Dev Health ACR",
    "version": "0.1.1-main.d753211fa980fcba497ba280bd5a37033d21fd70"
  },
  "tools": [
    {
      "description_digest": "sha256:4f99b9bc15b1b752b3732a0b76abec2d4b9682c1ce99834c24b72e6ba8fe96ca",
      "input_schema": {
        "$id": "https://contracts.fullchaos.dev/acr/v1/mcp_context_for_task_request.v1.schema.json",
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "additionalProperties": false,
        "description": "Input for the context_for_task tool. Only goal is required. Give a short, concrete description of the task you are about to do; add repository, scope, budget or requested_categories only when you need to override what the sidecar detects from the open workspace. Omitted optional fields take safe defaults. On a hosted server there is no workspace to detect: repository.slug is required, MCP roots, the working directory, Git and the local index are never consulted, and scope.include_changed_files=true without scope.files is refused; pass repository and, if wanted, scope.files.",
        "examples": [
          {
            "goal": "Add retry handling to the webhook sender"
          },
          {
            "goal": "Reduce flaky test failures in the billing service",
            "repository": {
              "slug": "acme/billing"
            },
            "requested_categories": [
              "state",
              "cause"
            ],
            "scope": {
              "files": [
                "src/billing/retry.go"
              ],
              "time_window_days": 90
            }
          }
        ],
        "properties": {
          "budget": {
            "additionalProperties": false,
            "description": "Optional size limits for the packet. Each value is clamped to what the service allows for your credential, so you can only ask for less than the maximum, never more.",
            "properties": {
              "max_items": {
                "description": "Maximum number of ranked context items, 1 to 50.",
                "maximum": 50,
                "minimum": 1,
                "type": "integer"
              },
              "max_output_tokens": {
                "description": "Approximate token ceiling for the packet, 500 to 16000.",
                "maximum": 16000,
                "minimum": 500,
                "type": "integer"
              },
              "max_serialized_bytes": {
                "description": "Byte ceiling for the whole serialized response, 8192 to 1048576. Content that does not fit is dropped, never cut mid-document.",
                "maximum": 1048576,
                "minimum": 8192,
                "type": "integer"
              }
            },
            "type": "object"
          },
          "goal": {
            "description": "What you are trying to do, in one or two plain sentences (for example a change you plan to make, or a problem you are diagnosing). Required. The packet is ranked against this text, so be specific about the area and the outcome.",
            "maxLength": 4000,
            "minLength": 1,
            "type": "string"
          },
          "repository": {
            "additionalProperties": false,
            "description": "The repository the task is about. On a hosted server it is required: the server cannot see your workspace, so omitting it returns a validation error that names repository.slug. On a local server it is optional and detected from the open workspace when omitted; set it to override that or when no workspace is open.",
            "properties": {
              "slug": {
                "description": "Repository in owner/name form, for example acme/billing. Exactly one slash and no spaces.",
                "maxLength": 512,
                "minLength": 1,
                "pattern": "^[^/\\s]+/[^/\\s]+$",
                "type": "string"
              }
            },
            "required": [
              "slug"
            ],
            "type": "object"
          },
          "requested_categories": {
            "description": "Optional. Kinds of context to prioritize: state (where things stand today), pressure (what is under strain), cause (why), evidence (the supporting records), action (suggested next steps). Omit for all.",
            "items": {
              "enum": [
                "state",
                "pressure",
                "cause",
                "evidence",
                "action"
              ]
            },
            "maxItems": 5,
            "type": "array",
            "uniqueItems": true
          },
          "scope": {
            "additionalProperties": false,
            "description": "Optional narrowing of what evidence is considered. Every field is optional; leave the object out to use defaults.",
            "properties": {
              "as_of": {
                "description": "Pin retrieval to a point in time (RFC 3339 date-time). Optional. Omit for current state.",
                "format": "date-time",
                "type": "string"
              },
              "branch": {
                "description": "Branch name to focus on. Optional.",
                "maxLength": 512,
                "type": "string"
              },
              "commit_sha": {
                "description": "Commit to focus on, as a full or abbreviated hex SHA (7 to 64 characters). Optional.",
                "pattern": "^[0-9a-fA-F]{7,64}$",
                "type": "string"
              },
              "files": {
                "description": "Repository-relative paths the task touches (up to 200, no duplicates). When set, these replace the automatic changed-files detection. On a hosted server this is how you supply changed files.",
                "items": {
                  "maxLength": 2048,
                  "minLength": 1,
                  "type": "string"
                },
                "maxItems": 200,
                "type": "array",
                "uniqueItems": true
              },
              "include_changed_files": {
                "description": "Whether to add the workspace's git-changed files to the scope. Omitted: sidecar default. true: include a bounded list of changed files when files is not set. false: never add them. Not available on a hosted server unless files is set: true without files returns a validation error; list the paths in files instead.",
                "type": "boolean"
              },
              "task_ref": {
                "description": "Free-form reference to the task, such as an issue key or URL, kept with the request for traceability. Optional; it does not select evidence by itself.",
                "maxLength": 1024,
                "type": "string"
              },
              "time_window_days": {
                "description": "How many trailing days of history to consider, from 1 to 365. Optional; omitted uses the service default.",
                "maximum": 365,
                "minimum": 1,
                "type": "integer"
              }
            },
            "type": "object"
          }
        },
        "required": [
          "goal"
        ],
        "title": "ACR MCP context_for_task Request v1",
        "type": "object"
      },
      "input_schema_digest": "sha256:96ec43cc057f355ff1947bff434e1ff7765757669b40bf705d57fe152db7cb70",
      "name": "context_for_task"
    },
    {
      "description_digest": "sha256:608784931bb56344a356cb3e1e96a837564e695d4872656ccd2bde348d37d43f",
      "input_schema": {
        "$defs": {
          "AnchorBoundReceipt": {
            "additionalProperties": false,
            "description": "A receipt selecting an anchor option the previous answer offered. The receipt_id must start with ancr_; a receipt with another prefix is rejected.",
            "properties": {
              "receipt_id": {
                "description": "The receipt_id of the chosen option, unchanged; it starts with ancr_.",
                "maxLength": 256,
                "minLength": 8,
                "pattern": "^ancr_",
                "type": "string"
              },
              "result_id": {
                "description": "The result_id of the answer the receipt came from, exactly as returned.",
                "maxLength": 256,
                "minLength": 8,
                "type": "string"
              }
            },
            "required": [
              "result_id",
              "receipt_id"
            ],
            "type": "object"
          },
          "BoundSubjectReceipt": {
            "additionalProperties": false,
            "description": "A receipt confirming a subject a previous answer committed to.",
            "properties": {
              "receipt_id": {
                "description": "The receipt_id from that answer's subject_receipts, unchanged.",
                "maxLength": 256,
                "minLength": 8,
                "type": "string"
              },
              "result_id": {
                "description": "The result_id of the answer the receipt came from, exactly as returned.",
                "maxLength": 256,
                "minLength": 8,
                "type": "string"
              }
            },
            "required": [
              "result_id",
              "receipt_id"
            ],
            "type": "object"
          },
          "CandidateBoundReceipt": {
            "additionalProperties": false,
            "description": "A receipt selecting a candidate option the previous answer offered. The receipt_id must start with candr_; a receipt with another prefix is rejected.",
            "properties": {
              "receipt_id": {
                "description": "The receipt_id of the chosen option, unchanged; it starts with candr_.",
                "maxLength": 256,
                "minLength": 8,
                "pattern": "^candr_",
                "type": "string"
              },
              "result_id": {
                "description": "The result_id of the answer the receipt came from, exactly as returned.",
                "maxLength": 256,
                "minLength": 8,
                "type": "string"
              }
            },
            "required": [
              "result_id",
              "receipt_id"
            ],
            "type": "object"
          },
          "ConversationTurn": {
            "additionalProperties": false,
            "description": "One previous turn of the conversation.",
            "properties": {
              "content": {
                "description": "The text of the turn.",
                "maxLength": 12000,
                "minLength": 1,
                "type": "string"
              },
              "created_at": {
                "description": "When the turn happened, as an RFC 3339 date-time.",
                "format": "date-time",
                "type": "string"
              },
              "role": {
                "description": "Who said it: user or assistant.",
                "enum": [
                  "user",
                  "assistant"
                ],
                "type": "string"
              },
              "turn_id": {
                "description": "Your own identifier for this turn. Unique within the conversation.",
                "maxLength": 256,
                "minLength": 1,
                "type": "string"
              }
            },
            "required": [
              "turn_id",
              "role",
              "content",
              "created_at"
            ],
            "type": "object"
          },
          "HandleBoundReceipt": {
            "additionalProperties": false,
            "description": "A receipt selecting a handle option the previous answer offered. The receipt_id must start with handr_; a receipt with another prefix is rejected.",
            "properties": {
              "receipt_id": {
                "description": "The receipt_id of the chosen option, unchanged; it starts with handr_.",
                "maxLength": 256,
                "minLength": 8,
                "pattern": "^handr_",
                "type": "string"
              },
              "result_id": {
                "description": "The result_id of the answer the receipt came from, exactly as returned.",
                "maxLength": 256,
                "minLength": 8,
                "type": "string"
              }
            },
            "required": [
              "result_id",
              "receipt_id"
            ],
            "type": "object"
          },
          "InvestigationBudget": {
            "additionalProperties": false,
            "description": "Optional limits on the size of the answer. Omitted fields take an agent-appropriate default, and every field is clamped to what the service grants your credential.",
            "properties": {
              "max_cohort_members": {
                "description": "Maximum number of members to list when the answer is about a cohort, 1 to 250.",
                "maximum": 250,
                "minimum": 1,
                "type": "integer"
              },
              "max_drivers": {
                "description": "Maximum number of principal drivers to return, 1 to 50.",
                "maximum": 50,
                "minimum": 1,
                "type": "integer"
              },
              "max_evidence_refs": {
                "description": "Maximum number of evidence references to return, 1 to 500.",
                "maximum": 500,
                "minimum": 1,
                "type": "integer"
              },
              "max_serialized_bytes": {
                "description": "Byte ceiling for the whole serialized response, 8192 to 1048576. Content that does not fit is dropped, never truncated into invalid JSON.",
                "maximum": 1048576,
                "minimum": 8192,
                "type": "integer"
              }
            },
            "type": "object"
          },
          "InvestigationScope": {
            "additionalProperties": false,
            "description": "Restricts the search to exact identifiers. It narrows what may be considered without saying what any subject is. Every list is optional and holds up to 200 unique values.",
            "properties": {
              "project_ids": {
                "description": "Project identifiers as the service uses them. Omit unless you obtained them from a previous answer.",
                "items": {
                  "maxLength": 256,
                  "minLength": 1,
                  "pattern": "^[^|]*$",
                  "type": "string"
                },
                "maxItems": 200,
                "type": "array",
                "uniqueItems": true
              },
              "repository_slugs": {
                "description": "Repository slugs in owner/name form, for example acme/billing.",
                "items": {
                  "maxLength": 512,
                  "minLength": 1,
                  "pattern": "^[^|]*$",
                  "type": "string"
                },
                "maxItems": 200,
                "type": "array",
                "uniqueItems": true
              },
              "team_ids": {
                "description": "Team identifiers as the service uses them. Omit unless you obtained them from a previous answer.",
                "items": {
                  "maxLength": 256,
                  "minLength": 1,
                  "pattern": "^[^|]*$",
                  "type": "string"
                },
                "maxItems": 200,
                "type": "array",
                "uniqueItems": true
              }
            },
            "type": "object"
          },
          "KindBoundReceipt": {
            "additionalProperties": false,
            "description": "A receipt selecting a kind option the previous answer offered. The receipt_id must start with kindr_; a receipt with another prefix is rejected.",
            "properties": {
              "receipt_id": {
                "description": "The receipt_id of the chosen option, unchanged; it starts with kindr_.",
                "maxLength": 256,
                "minLength": 8,
                "pattern": "^kindr_",
                "type": "string"
              },
              "result_id": {
                "description": "The result_id of the answer the receipt came from, exactly as returned.",
                "maxLength": 256,
                "minLength": 8,
                "type": "string"
              }
            },
            "required": [
              "result_id",
              "receipt_id"
            ],
            "type": "object"
          },
          "RelativeWindowID": {
            "description": "Named period ending at the time of the call: trailing_30d, trailing_90d, trailing_365d (the last 30, 90 or 365 days) or all_time (everything available).",
            "enum": [
              "trailing_30d",
              "trailing_90d",
              "trailing_365d",
              "all_time"
            ],
            "type": "string"
          },
          "RequestedEvidenceWindow": {
            "additionalProperties": false,
            "anyOf": [
              {
                "required": [
                  "relative_id"
                ]
              },
              {
                "required": [
                  "start",
                  "end"
                ]
              }
            ],
            "description": "The period you want covered. Give relative_id alone (trailing_30d, trailing_90d, trailing_365d, all_time) or both start and end. all_time takes no start or end; any other shape needs both start and end, never only one. The service decides how the window is labelled and how confident it is; you cannot set that.",
            "if": {
              "properties": {
                "relative_id": {
                  "const": "all_time"
                }
              },
              "required": [
                "relative_id"
              ]
            },
            "not": {
              "anyOf": [
                {
                  "not": {
                    "required": [
                      "end"
                    ]
                  },
                  "required": [
                    "start"
                  ]
                },
                {
                  "not": {
                    "required": [
                      "start"
                    ]
                  },
                  "required": [
                    "end"
                  ]
                }
              ],
              "description": "A start without an end, or an end without a start, is not accepted."
            },
            "properties": {
              "end": {
                "description": "End of the period, RFC 3339 date-time, after start. Give it together with start.",
                "format": "date-time",
                "type": "string"
              },
              "relative_id": {
                "$ref": "#/$defs/RelativeWindowID",
                "description": "A named period ending at the time of the call. Use this instead of start and end."
              },
              "start": {
                "description": "Start of the period, RFC 3339 date-time. Give it together with end.",
                "format": "date-time",
                "type": "string"
              }
            },
            "then": {
              "not": {
                "anyOf": [
                  {
                    "required": [
                      "start"
                    ]
                  },
                  {
                    "required": [
                      "end"
                    ]
                  }
                ]
              }
            },
            "type": "object"
          },
          "RequestedHandle": {
            "additionalProperties": false,
            "description": "One exact identifier you already know: a kind, a pattern_id naming how the value is written, and the value. The service checks the value against that pattern before using it. Known pattern_ids include pull_request_number (kind pull_request, value such as 532), work_item_ticket_key (kind work_item, the full ticket key as written) and ci_run_id (kind ci_pipeline_run, the run number). The list can grow, so pattern_id is free text and not an enum.",
            "properties": {
              "kind": {
                "$ref": "#/$defs/SubjectKind",
                "description": "The kind of subject the identifier names."
              },
              "pattern_id": {
                "description": "How the value is written. Examples: pull_request_number, work_item_ticket_key, ci_run_id. Not a closed list; an unknown pattern is refused or ignored.",
                "maxLength": 128,
                "minLength": 1,
                "type": "string"
              },
              "value": {
                "description": "The identifier itself in the form the pattern expects: digits for pull_request_number and ci_run_id (for example 532), the whole key for work_item_ticket_key.",
                "maxLength": 256,
                "minLength": 1,
                "type": "string"
              }
            },
            "required": [
              "kind",
              "pattern_id",
              "value"
            ],
            "type": "object"
          },
          "SubjectKind": {
            "description": "The kinds of subject the service knows: organization, team, project, repository, work_item (a ticket or issue), pull_request, deployment, incident, document, decision, episode (a recorded agent run), metric, pull_request_review, ci_pipeline_run and work_item_ref (a work item known only by a reference). All are accepted on the wire; only team, project, repository, incident and pull_request can be discovered as a cohort. work_item_ref is never answered on its own.",
            "enum": [
              "organization",
              "team",
              "project",
              "repository",
              "work_item",
              "pull_request",
              "deployment",
              "incident",
              "document",
              "decision",
              "episode",
              "metric",
              "pull_request_review",
              "ci_pipeline_run",
              "work_item_ref"
            ],
            "type": "string"
          },
          "WindowBoundReceipt": {
            "additionalProperties": false,
            "description": "A receipt selecting a window option the previous answer offered. The receipt_id must start with winr_; a receipt with another prefix is rejected.",
            "properties": {
              "receipt_id": {
                "description": "The receipt_id of the chosen option, unchanged; it starts with winr_.",
                "maxLength": 256,
                "minLength": 8,
                "pattern": "^winr_",
                "type": "string"
              },
              "result_id": {
                "description": "The result_id of the answer the receipt came from, exactly as returned.",
                "maxLength": 256,
                "minLength": 8,
                "type": "string"
              }
            },
            "required": [
              "result_id",
              "receipt_id"
            ],
            "type": "object"
          },
          "WindowConfirmationMode": {
            "description": "How to report a window the service inferred rather than received from you: headless (empty string means the same) reports it in the structured result only; nudge also adds a warning sentence asking you to confirm it.",
            "enum": [
              "",
              "headless",
              "nudge"
            ],
            "type": "string"
          }
        },
        "$id": "https://contracts.fullchaos.dev/acr/v1/mcp_investigate_question_request.v1.schema.json",
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "additionalProperties": false,
        "description": "Input for the investigate_question tool. Only question is required. Ask in plain language about one subject (a team, project, repository, pull request, incident) or about a set of them. Everything else is optional: leave scope, expected_kinds, subject_handles and windows out unless you already hold exact values, and let the service resolve names from the question. Follow-up turns pass back the receipts and result_id from the previous answer. This tool always investigates current state; use evidence_window to ask about a period. If a full_result does not fit the byte budget it is dropped and projection_budget.full_result_omitted is set; fetch it with investigation_result (if this server offers it) and the returned result_id.",
        "examples": [
          {
            "question": "Which teams look most under strain this month?"
          },
          {
            "expected_kinds": [
              "pull_request"
            ],
            "question": "Is pull request 532 ready to merge?",
            "subject_handles": [
              {
                "kind": "pull_request",
                "pattern_id": "pull_request_number",
                "value": "532"
              }
            ]
          },
          {
            "budget": {
              "max_serialized_bytes": 65536
            },
            "evidence_window": {
              "relative_id": "trailing_90d"
            },
            "question": "How has delivery changed for the payments project?"
          },
          {
            "parent_result_id": "res_4d1c9a72",
            "prior_window_receipts": [
              {
                "receipt_id": "winr_a1b2c3d4",
                "result_id": "res_4d1c9a72"
              }
            ],
            "question": "Use the trailing 90 days"
          }
        ],
        "properties": {
          "allow_clarification": {
            "description": "Optional. When true or omitted, the service may return status clarification_required with structure_needs that list the choices it needs from you. Set false to get the best available answer instead of a question back.",
            "type": "boolean"
          },
          "budget": {
            "$ref": "#/$defs/InvestigationBudget",
            "description": "Optional limits on the size of the answer. Omitted fields use defaults suited to agents. Every value is clamped to what the service grants your credential, so you can only ask for less than the maximum."
          },
          "conversation": {
            "description": "Optional previous turns of this conversation (oldest first, up to 50), so a short follow-up such as \"and last quarter?\" can be understood. Include user and assistant turns as they were said. Confirmed subjects travel through the receipts and parent_result_id, not through this list.",
            "items": {
              "$ref": "#/$defs/ConversationTurn"
            },
            "maxItems": 50,
            "type": "array"
          },
          "evidence_window": {
            "$ref": "#/$defs/RequestedEvidenceWindow",
            "description": "Optional period the answer should cover. Give either relative_id (trailing_30d, trailing_90d, trailing_365d or all_time) or both start and end. This is your stated intent; the service may still report the window it used and ask you to confirm it (see window_clarification and structure_needs.window_options)."
          },
          "expected_kinds": {
            "description": "Optional hint about what kinds of subject the question is about, for example [\"team\"] or [\"pull_request\"]. Use it when the question is ambiguous about kind. It states your intent, not a fact about any subject. Every value in the SubjectKind list is accepted, but only team, project, repository, incident and pull_request can be discovered as a cohort (a \"which ...\" question over many); other kinds are answered when you name the specific subject.",
            "items": {
              "$ref": "#/$defs/SubjectKind"
            },
            "maxItems": 15,
            "type": "array",
            "uniqueItems": true
          },
          "include_full_result": {
            "description": "Optional, default false. When true, also return the full canonical result next to the bounded answer, if it fits the byte budget. Otherwise leave it out and call investigation_result (if this server offers it) with the result_id only when you need more detail.",
            "type": "boolean"
          },
          "parent_result_id": {
            "description": "The result_id of the answer this turn follows. Optional. It links the turns of one conversation; it does not by itself bind that answer's subjects into this turn, use the prior_*_receipts for that.",
            "maxLength": 256,
            "minLength": 8,
            "type": "string"
          },
          "prior_anchor_receipts": {
            "description": "Answers a clarification about which named subject you meant. Take the receipt_id (starts with ancr_) of the option you choose from structure_needs.anchor_options. Receipts returned by the previous answer. Copy each entry unchanged from that answer, with the result_id it came from.",
            "items": {
              "$ref": "#/$defs/AnchorBoundReceipt"
            },
            "maxItems": 20,
            "type": "array"
          },
          "prior_candidate_receipts": {
            "description": "Chooses among candidate subjects the service offered when several matched. Take the receipt_id (starts with candr_) of the option you choose from structure_needs.candidate_options. Receipts returned by the previous answer. Copy each entry unchanged from that answer, with the result_id it came from.",
            "items": {
              "$ref": "#/$defs/CandidateBoundReceipt"
            },
            "maxItems": 20,
            "type": "array"
          },
          "prior_handle_receipts": {
            "description": "Confirms an identifier the service offered, such as a pull request number. Take the receipt_id (starts with handr_) of the option you choose from structure_needs.handle_options. Receipts returned by the previous answer. Copy each entry unchanged from that answer, with the result_id it came from.",
            "items": {
              "$ref": "#/$defs/HandleBoundReceipt"
            },
            "maxItems": 20,
            "type": "array"
          },
          "prior_kind_receipts": {
            "description": "Answers a clarification about what kind of thing you meant. Take the receipt_id (starts with kindr_) of the kind option you choose from structure_needs.kind_options. Receipts returned by the previous answer. Copy each entry unchanged from that answer, with the result_id it came from.",
            "items": {
              "$ref": "#/$defs/KindBoundReceipt"
            },
            "maxItems": 20,
            "type": "array"
          },
          "prior_subject_receipts": {
            "description": "Confirms subjects a previous answer committed to, so this turn keeps investigating the same subjects. Take entries from that answer's subject_receipts. Receipts returned by the previous answer. Copy each entry unchanged from that answer, with the result_id it came from.",
            "items": {
              "$ref": "#/$defs/BoundSubjectReceipt"
            },
            "maxItems": 20,
            "type": "array"
          },
          "prior_window_receipts": {
            "description": "Confirms a time window the service offered. Take the receipt_id (starts with winr_) of the option you choose from structure_needs.window_options. Receipts returned by the previous answer. Copy each entry unchanged from that answer, with the result_id it came from.",
            "items": {
              "$ref": "#/$defs/WindowBoundReceipt"
            },
            "maxItems": 20,
            "type": "array"
          },
          "question": {
            "description": "The question in natural language, for example \"which teams need attention?\" or \"what is blocking the payments project?\". Name the subject and, if it matters, the period. Required. Write it as a person would ask it; do not pass code or instructions to the service.",
            "maxLength": 8000,
            "minLength": 1,
            "type": "string"
          },
          "scope": {
            "$ref": "#/$defs/InvestigationScope",
            "description": "Optional. Restricts the search to exact identifiers you already hold. It does not say what any subject is. Omit it when you only know names; there is no tool that lists valid ids, so put names in the question and let the service resolve them."
          },
          "subject_handles": {
            "description": "Optional exact identifiers you already know, each as a kind, a pattern_id and a value (up to 20). Example: {\"kind\":\"pull_request\",\"pattern_id\":\"pull_request_number\",\"value\":\"532\"}. Usually unnecessary: the service also recognizes identifiers written in the question text, and it offers matches back in structure_needs.handle_options.",
            "items": {
              "$ref": "#/$defs/RequestedHandle"
            },
            "maxItems": 20,
            "type": "array",
            "uniqueItems": true
          },
          "window_confirmation_mode": {
            "$ref": "#/$defs/WindowConfirmationMode",
            "description": "Optional. How an inferred, unconfirmed window is reported: headless (the default when empty or omitted) reports it in the structured result; nudge also adds a sentence in the warnings asking you to confirm it. Does not change which window is used."
          }
        },
        "required": [
          "question"
        ],
        "title": "ACR MCP investigate_question Request v1",
        "type": "object"
      },
      "input_schema_digest": "sha256:a8922d094160a781e0631102f16c9ca7c25b8db529356577ae1be2ab85cb61e3",
      "name": "investigate_question"
    },
    {
      "description_digest": "sha256:b1e295e58079ec7339b7191e9f9913bdc8193167ce6770547a813cace5796183",
      "input_schema": {
        "$id": "https://contracts.fullchaos.dev/acr/v1/mcp_investigation_result_request.v1.schema.json",
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "additionalProperties": false,
        "description": "Input for the investigation_result tool. Exactly one field, result_id, is accepted and required.",
        "examples": [
          {
            "result_id": "res_4d1c9a72"
          }
        ],
        "properties": {
          "result_id": {
            "description": "The result_id of a previous investigate_question answer, exactly as returned. Opaque: do not build or parse it. Access is re-checked against your credential on every call, so an id from another organization or one you are not allowed to read is refused.",
            "maxLength": 256,
            "minLength": 8,
            "type": "string"
          }
        },
        "required": [
          "result_id"
        ],
        "title": "ACR MCP investigation_result Request v1",
        "type": "object"
      },
      "input_schema_digest": "sha256:da92f58ed993f83ac58b09184fa538d6c0e7d00ef6f692860b15907abd650517",
      "name": "investigation_result"
    },
    {
      "description_digest": "sha256:d280080904b79b3d47d1cd3ee5f08d7a0587152a2df0de79dbc3bc0013755077",
      "input_schema": {
        "$id": "https://contracts.fullchaos.dev/acr/v1/mcp_source_evidence_request.v1.schema.json",
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "additionalProperties": false,
        "description": "Input for the source_evidence tool. Exactly one field, evidence_ref_id, is accepted and required.",
        "examples": [
          {
            "evidence_ref_id": "ev_2f9c81ab"
          }
        ],
        "properties": {
          "evidence_ref_id": {
            "description": "An evidence reference exactly as a previous context_for_task or investigate_question response returned it (its evidence_ref_ids, or an evidence reference on a context item). Opaque: pass it back unchanged; never build or parse one yourself.",
            "maxLength": 256,
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "evidence_ref_id"
        ],
        "title": "ACR MCP source_evidence Request v1",
        "type": "object"
      },
      "input_schema_digest": "sha256:a45df8147c3186d6724cd5d36b0eac10653aa76b8f36451122bb64ea861ad6fb",
      "name": "source_evidence"
    },
    {
      "description_digest": "sha256:00",
      "input_schema": {
        "type": "object"
      },
      "input_schema_digest": "sha256:00",
      "name": "kill_proof_extra_tool"
    }
  ]
}
